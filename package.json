{
  "name": "nweii-tools-mcp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Personal MCP toolkit — wraps local CLIs (bird, …) as MCP tools for remote use from Claude.ai and other MCP clients.",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "@steipete/bird": "^0.8.0",
    "express": "^4.21.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^22.0.0"
  }
}
